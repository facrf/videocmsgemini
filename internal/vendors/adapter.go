package vendors

import (
	"videocms/internal/camera"
)

// Re-export interface aliases for convenience if needed
type CameraAdapter = camera.CameraAdapter
type DeviceInfo = camera.DeviceInfo
type Credentials = camera.Credentials
